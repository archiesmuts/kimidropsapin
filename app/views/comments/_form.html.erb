<%= form_with(model: [@post, @post.comments.build], id: :new_comment, class: :new_comment) do |form| %>

  <div class="errors"> </div>
  <%= form.hidden_field :post_id %>
  <div class="form-group">
    <%= form.text_field :name, class: "form-control", placeholder: "Tell us your name", required: true %>
  </div>

  <!-- <div class="form-group">
    <%#= form.text_field :email, class: "form-control", placeholder: "Email is optional" %>
    <small>Email is optional. However, we keep it private if you do want to share it.</small>
  </div> -->

  <div class="form-group">
    <%#= form.text_area :body, class: "form-control", placeholder: "Leave your comment here", required: true %>
    <%= form.trix_editor :body %>
  </div>


  <div class="actions">
    <%= form.submit "Post your comment", class: "btn btn-primary" %>
  </div>
<% end %>
