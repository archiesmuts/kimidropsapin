<!-- ====== Carousel ====== -->
<div class="mb-10">

  <%= render "carousel" %>
</div>
<!-- Blog Grid Overlap Blocks -->
<div class="container-fluid px-30 pt-100">
  <div class="d-flex flex-wrap justify-content-center align-items-stretch mt-100">
      <%= render @posts %>
  </div>
</div>
