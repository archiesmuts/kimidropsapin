<div class="jumbotron">
  <div class="row">
    <div class="col-lg-10 offset-lg-1">

      <h1>Posts</h1>
      <h2>Hello Kimberley!  This is the first draft.</h2>
      <p class="lead">
        Just a reminder that as the owner of the blog you have to sign up first.
        You can create, edit and delete posts only after you sign in with your
        email and password.
      </p>
      <p class="lead">No sign-in is required for viewing posts and leaving comments.</p>

      <%= link_to 'Create a New Post', new_post_path, class: "btn btn-lg btn-primary" %>

    </div>
  </div>
</div>

<div class="row">
  <div class="col-xl-10 offset-xl-1">
    <table class="table mt-3">
      <tbody>
        <% @posts.each do |post| %>
          <tr>
            <td>
              <h1><%= post.title %></h1>
              <p class="lead">
                <%= sanitize(post.body.truncate(500)) %>
                <%= link_to 'Read more', post, class: "btn btn-link" %>
              </p>
              <p>
                <em>
                  <%= fa_icon("calendar-alt") %>&nbsp;Posted&nbsp;<%= time_ago_in_words(post.created_at) %>&nbsp;ago&nbsp;
                  <%= fa_icon("comments") %>&nbsp;<%= pluralize(post.comments.size, 'comment') %>
                </em>
              </p>
              <p>
                <%= link_to 'Show', post, class: "btn btn-link" %>
                <%= link_to 'Edit', edit_post_path(post), class: "btn btn-link" %>
                <%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %>
              </p>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
